{
  "name": "plus500",
  "version": "1.0.0",
  "description": "",
  "main": "build/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "copyfiles main.js app/**/*.* build && babel build/app/**/*.js --presets react --out-dir . && babel *.js build --presets babel-preset-es2015 --out-dir .",
    "start": "npm run build && electron ./build/main.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "electron": "^1.4.13",
    "react": "^15.4.2",
    "react-dom": "^15.4.2",
    "request": "^2.79.0",
    "request-promise": "^4.1.1"
  },
  "devDependencies": {
    "babel-core": "^6.21.0",
    "babel-preset-es2015": "^6.18.0",
    "babel-preset-react": "^6.16.0"
  }
}
